#include <stdio.h>
int main()
{
    int a, b;
  //  int vikash kr;      //not alloweed
    //int 7;              //not allowed
    int vikash_bytes; //allowed
// // Rules for naming variables in C
// 1. First Character must be an alphabet or underscore (-)
// 2. No Commas, blanks allowed.
// 3. No special symbol other than () allowed.
// 4. Variable names are case sensitive
// We must create meaningful variable names in our programs This
// enhances readability of our programs.
    printf("the value of a =%d", a);
    return 0;
}