#include <stdio.h>
int main()
{
    // char st[] = {'a', 'b', 'c', '\0'};
    char st[] = "vikash"; // same as above code
    for (int i = 0; i < 6; i++)
    {
        printf(" character is %c \n", st[i]);
    }

    return 0;
}