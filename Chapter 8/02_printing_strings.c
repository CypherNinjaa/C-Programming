#include <stdio.h>
int main()
{
    // char st[] = {'a', 'b', 'c', '\0'};
    char st[] = "vikash"; // same as above code

    // printing by loop

    /* for (int i = 0; i < 6; i++)
     {
         printf("%c", st[i]);
     }*/

    // printing directly

    printf("%s", st);

    return 0;
}